// Animation timing constants (single source of truth)
export const CARD_ANIMATION_DURATION = 800; // ms - flying card animation duration
export const CARD_APPEAR_TIME = 700; // ms - slightly before animation completes for smooth transition

// Gap between dealing each card (ensures previous card lands before next starts)
export const CARD_DEAL_GAP = 250; // ms - buffer after animation completes before next card

// Split animation delays
export const SPLIT_SEPARATION_DELAY = 500; // ms - delay after separating cards
export const SPLIT_CARD_DEAL_DELAY = 600; // ms - delay between dealing to each split hand
